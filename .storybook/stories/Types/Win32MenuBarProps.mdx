import { Meta, ArgTypes } from '@storybook/addon-docs/blocks';
import * as meta from '../../../src/components/MenuBar/MenuBar.stories.ts';

<Meta title='Types/Win32MenuBarProps' />

# `Win32MenuBarProps` Interface

The `Win32MenuBarProps` interface describes the `Win32MenuBar` component properties.


<ArgTypes of={meta} />
