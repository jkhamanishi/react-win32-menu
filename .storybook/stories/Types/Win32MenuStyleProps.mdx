import { Meta, ArgTypes } from '@storybook/addon-docs/blocks';
import * as meta from '../../stories/FullMenuBar/FullMenuBar.stories.ts';

<Meta title='Types/Win32MenuStyleProps' />

# `Win32MenuStyleProps` Type

The `Win32MenuStyleProps` type describes the `style` property of the `Win32MenuBar` component.

<ArgTypes of={meta} />
