import { Meta, ArgTypes } from '@storybook/addon-docs/blocks';
import * as MenuItem from '../../../src/components/MenuItem/MenuItem.stories.ts';

<Meta title='Types/MenuItemProps' />

# `MenuItemProps` Interface

The `MenuItemProps` interface describes the `MenuItem` component properties.


<ArgTypes of={MenuItem} />
